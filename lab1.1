#!/bin/bash
if [[ "$1" == "?" ]]
then echo "type 2 args for compare:"lab1.1 arg1 arg 2", if they are equals script will print "equals" else will print "not equals""
fi
if [[ "$1" == "$2" ]]
then echo "equals"
else echo "not equals"
fi

